export type NavItem = {
  title: string;
  type: string;
};

export type ServiceCardProps = {
  imgSrc: string;
  title: string;
  description: string;
};

export type Heading = {
  subHeading: string;
  heading: string;
};

export type DestionationInfo = {
  imgSrc: string;
  title: string;
  trip: string;
  dollars: string;
};
